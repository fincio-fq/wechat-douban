<swiper  interval="5000" duration="1000">
  <block wx:for="{{movies}}" wx:for-index="index">
    <swiper-item>
      <image src="{{item.images.large}}" class="slide-image" mode="aspectFill" width="100%" height="100%"/>
      <button class="btn" bindtap="start" wx:if="{{index == movies.length - 1}}">立即体验</button>
    </swiper-item>
  </block>
</swiper>
